@if (Request.IsAuthenticated)
{
    string controller = ViewBag.ControllerActive;
    string action = ViewBag.ActionActive;
    using (Html.BeginForm("LogOut", "Account", new { area = "", urlRequest = Request.Url.AbsoluteUri }, FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" }))
    {
        @Html.AntiForgeryToken()
        @*<ul class="nav navbar-nav navbar-right">
            <li>
                <p class="navbar-text">@string.Format("Bienvenido, {0}!", User.Identity.Name.Split('|')[3].Trim())</p>
            </li>

            <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">Perfil<b class="caret"></b></a>
                <ul class="dropdown-menu">
                    @Html.MenuItem("Packing List - Evaluación", "IndexAprobar", "PackingList", action, controller)
                    @Html.MenuItem("Cambiar Contraseña", "Modificar_Clave", "Account", action, controller)
                    @Html.MenuItem("Cerrar sesión", "Logout", "Account", action, controller)
                </ul>
            </li>
        </ul>*@
        }
        }
        else
        {
        using (Html.BeginForm("LogOut", "Account", new { area = "", urlRequest = Request.Url.AbsoluteUri }, FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" }))
        {
        @Html.AntiForgeryToken()

        <ul class="nav navbar-nav navbar-right">
            <li>
                @Html.ActionLink("Ingresar al SIGESDOC", "Login", "Account", new { area = "" }, null)
            </li>
        </ul>
        }
        }
